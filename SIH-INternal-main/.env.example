# Server and runtime
PORT=3000
NODE_ENV=development

# Database: PostgreSQL connection URL (Neon)
DATABASE_URL=postgresql://postgres:password@localhost:5432/innoprocure

# Authentication & Security
JWT_SECRET=innovprocure_enterprise_jwt_super_secure_secret_key_2026
JWT_EXPIRES_IN=1d
CORS_ORIGINS=http://localhost:3000

# Google Gemini API key (kept strictly server-side)
GEMINI_API_KEY=

# Storage Provider
STORAGE_PROVIDER=local
UPLOAD_DIR=./uploads

